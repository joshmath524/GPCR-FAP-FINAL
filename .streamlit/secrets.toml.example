# Copy to Streamlit Cloud → App settings → Secrets (do not commit secrets.toml).

DATA_ZIP_URL = "https://drive.google.com/uc?export=download&id=YOUR_GOOGLE_DRIVE_FILE_ID"
DATA_DIR = "runtime_data"

# Only if the zip has one top-level folder before Josh_Receptor_Features / ML_code:
# DATA_EXTRACTED_SUBDIR = "GPCRtryagain - Delete - Copy"
